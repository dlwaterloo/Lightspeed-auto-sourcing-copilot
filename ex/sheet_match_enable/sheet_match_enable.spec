# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['sheet_match_enable.py'],
    pathex=[],
    binaries=[],
    datas=[('Peer Funds 20250120-New Investment.xlsx', '.'), ('Param_enable.xlsx', '.'), ('PF Tracked List For Match.xlsx', '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='sheet_match_enable',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
